meta {
  name: ❌ Employee CANNOT create clock for others
  type: http
  seq: 3
}

post {
  url: {{base_url}}/clocks
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{employee_token}}
}

body:json {
  {
    "time": "2025-10-10T09:00:00+00:00",
    "status": true,
    "userId": 46
  }
}

tests {
  test("Status should be 403", function() {
    expect(res.status).to.equal(403);
  });
}

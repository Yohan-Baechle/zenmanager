meta {
  name: ✅ Employee can view own data
  type: http
  seq: 1
}

get {
  url: {{base_url}}/users/36
  body: none
  auth: bearer
}

auth:bearer {
  token: {{employee_token}}
}

tests {
  test("Status should be 200", function() {
    expect(res.status).to.equal(200);
  });
}

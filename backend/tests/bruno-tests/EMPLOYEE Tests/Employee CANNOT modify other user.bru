meta {
  name: ❌ Employee CANNOT modify other user
  type: http
  seq: 5
}

put {
  url: {{base_url}}/users/46
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{employee_token}}
}

body:json {
  {
    "firstName": "Hacked"
  }
}

tests {
  test("Status should be 403", function() {
    expect(res.status).to.equal(403);
  });
}

meta {
  name: ❌ Employee CANNOT view others clocks
  type: http
  seq: 4
}

get {
  url: {{base_url}}/users/46/clocks
  body: none
  auth: bearer
}

auth:bearer {
  token: {{employee_token}}
}

tests {
  test("Status should be 403", function() {
    expect(res.status).to.equal(403);
  });
}

meta {
  name: ✅ Manager can view team member
  type: http
  seq: 1
}

get {
  url: {{base_url}}/users/3
  body: none
  auth: bearer
}

auth:bearer {
  token: {{manager_dev_token}}
}

tests {
  test("Status should be 200", function() {
    expect(res.status).to.equal(200);
  });
}
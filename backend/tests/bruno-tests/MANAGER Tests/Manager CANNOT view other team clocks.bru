meta {
  name: ❌ Manager CANNOT view other team clocks
  type: http
  seq: 3
}

get {
  url: {{base_url}}/users/46/clocks
  body: none
  auth: bearer
}

auth:bearer {
  token: {{manager_dev_token}}
}

tests {
  test("Status should be 403", function() {
    expect(res.status).to.equal(403);
  });
}

meta {
  name: ✅ Admin can modify any user
  type: http
  seq: 2
}

put {
  url: {{base_url}}/users/38
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{admin_token}}
}

body:json {
  {
    "firstName": "Modified By Admin"
  }
}

tests {
  test("Status should be 200", function() {
    expect(res.status).to.equal(200);
  });
}
